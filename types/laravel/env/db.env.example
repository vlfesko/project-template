# MariaDB init vars — values must be literals (not ${X_*} references)
# Copy to env/db.env and set the same values as X_DB_* in .env

MYSQL_DATABASE=laravel
MYSQL_USER=laravel
MYSQL_PASSWORD=laravel
MYSQL_ROOT_PASSWORD=password
