PHP_EXTENSIONS_DISABLE=xhprof,spx
PHP_MAIL_MIXED_LF_AND_CRLF=On
PHP_FPM_USER=wodby
PHP_FPM_GROUP=wodby
# Let PHP-FPM workers inherit the container environment (DB_*, APP_*, …) set in
# compose.yaml. Without this, FPM clears the env and Laravel falls back to src/.env.
PHP_FPM_CLEAR_ENV=no
